// AuthRouter in _layout.tsx handles all navigation logic.
// This screen renders nothing — it exists only as the root entry point.
import { View } from "react-native";
export default function Index() {
  return <View style={{ flex: 1, backgroundColor: "#0F1117" }} />;
}
